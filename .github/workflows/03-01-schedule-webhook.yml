name: 03-01 - Webhook

on:
    repository_dispatch:
        types: [ejecutar_cron]

jobs:
    schedule:
        runs-on: ubuntu-24.04
        steps:
            - name: Saludar
              run: echo "Hola desde Github Actions"

# curl -L \
#   -X POST \
#   -H "Accept: application/vnd.github+json" \
#   -H "Authorization: Bearer <YOUR-TOKEN>" \
#   -H "X-GitHub-Api-Version: 2026-03-10" \
#   https://api.github.com/repos/<OWNER>/<REPO>/dispatches \
#   -d '{"event_type":"<on-demand-test>","client_payload":{"unit":false,"integration":true}}'

# Response status: 204

# Token: Settings (User) > Developer settings > Personal Access Token > Fine-grained token
#      Generate token > Name; Only select repositories; permisions: contents (Read and write)